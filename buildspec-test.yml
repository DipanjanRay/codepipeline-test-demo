version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install pytest

  build:
    commands:
      - echo "Environment=$Environment"
      - echo "TestSuite=$TestSuite"
      - pytest -v
